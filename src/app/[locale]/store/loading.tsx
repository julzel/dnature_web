import { FC } from 'react';

const Loading: FC = () => {
  return (
    <div>
      <h1>Loading...</h1>
      {/* Add your loading animation or spinner here */}
    </div>
  );
};

export default Loading;
