import { Center } from "@chakra-ui/react";
import Title from "@/components/Title";

const Hero = () => {
  return (
    <Center h='calc(100dvh - 164px)'>
      <Title transform='uppercase' type='banner'>
        DNAture
      </Title>
    </Center>
  );
}
 
export default Hero;